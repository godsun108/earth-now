const $=s=>document.querySelector(s),statusEl=$('#status'),card=$('#card'),clock=$('#clock'),legend=$('#legend');
const colors={quake:'#f4f7fa',wildfire:'#ff7043',storm:'#66c7ff',volcano:'#ffc857'};
const globe=Globe()($('#globe')).backgroundColor('#020408').globeImageUrl('https://unpkg.com/three-globe/example/img/earth-night.jpg').bumpImageUrl('https://unpkg.com/three-globe/example/img/earth-topology.png').showAtmosphere(true).atmosphereColor('#6f8fa8').atmosphereAltitude(.16).pointLat('lat').pointLng('lng').pointAltitude(d=>d.kind==='quake'?Math.max(.012,(d.mag||0)*.008):.025).pointRadius(d=>d.kind==='quake'?Math.max(.05,(d.mag||0)*.035):.13).pointColor(d=>colors[d.kind]||'#fff').pointLabel(()=>'').ringsData([]).ringLat('lat').ringLng('lng').ringColor(d=>()=>colors[d.kind]||'#fff').ringMaxRadius(d=>d.kind==='quake'?Math.max(.6,(d.mag||0)*.32):1.2).ringPropagationSpeed(.7).ringRepeatPeriod(1500).onPointClick(flyTo);
globe.controls().autoRotate=true;globe.controls().autoRotateSpeed=.16;globe.pointOfView({lat:18,lng:-25,altitude:2.15});
let all=[],enabled=new Set(['quake','wildfire','storm','volcano']);
const safe=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const age=t=>{if(!t)return'UNKNOWN AGE';const m=Math.max(0,Math.floor((Date.now()-t)/60000));return m<60?m+' MIN AGO':m<1440?Math.floor(m/60)+'H '+m%60+'M AGO':Math.floor(m/1440)+'D AGO'};
function render(){const shown=all.filter(d=>enabled.has(d.kind));globe.pointsData(shown);globe.ringsData(shown.filter(d=>(d.kind==='quake'&&d.mag>=4.5)||d.kind==='storm'));const counts={};shown.forEach(d=>counts[d.kind]=(counts[d.kind]||0)+1);legend.textContent=['quake','wildfire','storm','volcano'].filter(k=>enabled.has(k)).map(k=>k.toUpperCase()+' '+(counts[k]||0)).join(' · ')||'ALL LAYERS HIDDEN';statusEl.textContent=shown.length+' CURRENT OBSERVATIONS · USGS + NASA'}
function show(d){card.classList.remove('hidden');const metric=d.kind==='quake'?'M '+d.mag.toFixed(1):d.kind.toUpperCase();card.innerHTML='<div class="kind">'+d.kind.toUpperCase()+' · '+safe(d.source)+'</div><div class="mag">'+metric+'</div><h2>'+safe(d.title)+'</h2><p>'+(d.kind==='quake'?'DEPTH '+d.depth.toFixed(1)+' KM · ':'')+age(d.time)+'<br>OBSERVED '+(d.time?new Date(d.time).toLocaleString():'time not supplied')+'<br>SOURCE · '+safe(d.source)+'</p>'}
function flyTo(d){globe.controls().autoRotate=false;globe.pointOfView({lat:d.lat,lng:d.lng,altitude:.82},1400);show(d)}
async function quakes(){const r=await fetch('https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/all_day.geojson',{cache:'no-store'});if(!r.ok)throw Error('USGS '+r.status);const j=await r.json();return j.features.map(f=>({kind:'quake',lat:f.geometry.coordinates[1],lng:f.geometry.coordinates[0],depth:f.geometry.coordinates[2],mag:Number(f.properties.mag)||0,title:f.properties.place||'Earthquake',time:f.properties.time,source:'USGS'}))}
function eonetKind(cats){const s=cats.map(c=>c.id+' '+c.title).join(' ').toLowerCase();if(s.includes('wildfire'))return'wildfire';if(s.includes('storm'))return'storm';if(s.includes('volcano'))return'volcano';return null}
async function eonet(){const r=await fetch('https://eonet.gsfc.nasa.gov/api/v3/events?status=open&limit=500',{cache:'no-store'});if(!r.ok)throw Error('EONET '+r.status);const j=await r.json();return j.events.flatMap(e=>{const kind=eonetKind(e.categories||[]);if(!kind||!e.geometry?.length)return[];const g=e.geometry[e.geometry.length-1],c=g.coordinates;if(g.type!=='Point'||!Array.isArray(c))return[];return[{kind,lat:c[1],lng:c[0],title:e.title||'Natural event',time:Date.parse(g.date)||null,source:'NASA EONET'}]})}
async function load(){statusEl.textContent='SYNCING PLANETARY FEEDS…';const results=await Promise.allSettled([quakes(),eonet()]);all=results.flatMap(r=>r.status==='fulfilled'?r.value:[]);render();if(results.some(r=>r.status==='rejected'))statusEl.textContent+=' · ONE FEED UNAVAILABLE'}
document.querySelectorAll('.layer').forEach(b=>b.onclick=()=>{const k=b.dataset.layer;enabled.has(k)?enabled.delete(k):enabled.add(k);b.classList.toggle('active',enabled.has(k));render()});
$('#random').onclick=()=>{const pool=all.filter(d=>enabled.has(d.kind));if(!pool.length)return;const ranked=[...pool].sort((a,b)=>score(b)-score(a)).slice(0,Math.min(45,pool.length));flyTo(ranked[Math.floor(Math.random()*ranked.length)])};
function score(d){const fresh=d.time?Math.max(0,7-(Date.now()-d.time)/86400000):0;return(d.kind==='quake'?(d.mag||0)*2:5)+fresh}
setInterval(()=>clock.textContent=new Date().toISOString().replace('T',' · ').slice(0,21)+' UTC',1000);load();setInterval(load,300000);